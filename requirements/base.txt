# =========================================================
# Enterprise AI Lab - Production Requirements (Python 3.11)
# Stable LangChain + Ollama + Tool Agent Stack
# =========================================================

# =========================
# Core HTTP + Configuration
# =========================
requests==2.34.2
# HTTP client for external APIs (weather, web_search, etc.)
# Stable, production-safe, widely used
PyYAML==6.0.2
# YAML config loader (config.yaml support)
python-dotenv==1.2.2
# Loads environment variables from .env files

# =========================
# LLM SDKs (Optional / Future Flexibility)
# =========================
openai==1.82.0
# OpenAI SDK (optional if you ever switch models or compare outputs)
anthropic>=0.25.0
# Anthropic Claude SDK (optional multi-model support)

# =========================
# LangChain Stable Stack (Enterprise Pinning)
# =========================
langchain==0.3.30
# Core LangChain framework
# Provides AgentExecutor, initialize_agent, tools, prompts
# STABLE version for production agent + tool workflows
langchain-core==1.3.3
# RESOLVED CONFLICT: Bumped from 0.2.35 to 0.2.43 to satisfy 
# langchain-text-splitters requirement (requires >=0.2.38)
langchain-community==0.3.27
# Community tools + integrations
# Includes standard tool implementations and connectors
langchain-ollama==0.1.0
# Ollama integration for local LLMs (ChatOllama wrapper)
# Enables running models like qwen2.5 locally via Ollama
langchain-text-splitters==1.1.2
# Text chunking utilities (only needed if you add RAG later)

# =========================
# Observability / Logging
# =========================
structlog==22.1.0
# Structured JSON logging for debugging + production tracing
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-instrumentation-asyncio==0.55b1
# Distributed tracing + observability for latency debugging

# =========================
# Performance / Utilities
# =========================
cachetools==5.3.3
# In-memory caching (useful for tool response caching)
tiktoken==0.7.0
# Token counting for telemetry and LLM usage tracking
duckduckgo-search==3.9.3
# Free web search API (used by web_search tool implementation)